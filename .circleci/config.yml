version: 2
jobs:
  defaults:
    docker:
      - image: circleci/node:8-browsers
    working_directory: ~/
  build:
    steps:
      - checkout
      - run:
          name: install
          command: npm install
      - run:
          name: start
          command: npm start
  test:
    steps:
      - checkout
      - run:
          name:install
          command: npm install
      - run:
          name: start
          command: npm start
      - run:
          name: curl
          command: curl -i localhost:8080
