version: 2
jobs:
  build:
    working_directory:/usr/src/app
    docker:
      - image: circleci/node:8-browsers
    steps:
      - checkout
      - run:
          name: start
          command: npm start 
