version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8-browsers
    working_directory: ~/app
    steps:
      - checkout
      - run: npm install
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
